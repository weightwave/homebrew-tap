# Homebrew Tap for Taskcast

Install [Taskcast](https://github.com/weightwave/taskcast) via Homebrew:

```bash
brew tap weightwave/tap
brew install taskcast
```

## What is Taskcast?

Taskcast is a unified long-lifecycle task tracking service for LLM streaming, agents, and async workloads.